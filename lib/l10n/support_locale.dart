import 'package:flutter/material.dart';
import 'package:nobita/enums/enum_languages.dart';

class L10n {
  static List<Locale> support = [
    Locale(EnumLanguages.vi.name),
    Locale(EnumLanguages.en.name)
  ];
}
